name: diagnos

data_root: /home/bliu/work/Data/diagnos_dr
batch_size: 16

object:
  test:
    _target_: retinal.data.diagnos.get_data_loader
    data_root: ${data.data_root}
    batch_size: ${data.batch_size}
    split: test 